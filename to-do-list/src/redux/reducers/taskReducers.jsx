import {
  ADD_TODO,
  TODO_TOGGLE,
  COLOR_SELECT,
  DELETE_TODO,
  ALL_COMPLETE_TODO,
  CLEAR_TODO,
} from '../../actions/type';

export default function taskReducers(task, action) {
  const { type, payload } = action; // what is this for?
  // switch (type){
  //     case ADD_TODO
  // }
}
